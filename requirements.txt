# Core runtime
fastapi==0.112.2
uvicorn==0.30.6
# aiogram/pydantic subidos por el spike: google-genai (File Search) exige
# pydantic>=2.9, y aiogram 3.7.0 fijaba pydantic<2.8. Validado que el bot
# importa OK con aiogram 3.29.1. Revisar el runtime del bot antes de producción.
aiogram==3.29.1
python-dotenv==1.1.1
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1  # >=0.28.1 lo exige google-genai; <1 lo exige openai. Compatible con ambos.

# LLM + embeddings
openai==1.109.1
tiktoken==0.7.0
# SPIKE Gemini File Search (rama test/gemini-file-search-spike). OJO: arrastra
# pydantic>=2.8, por encima del pin de aiogram (<2.8); en pruebas funciona, pero
# revisar antes de llevar a producción.
google-genai==2.10.0

# LLM orchestration
langgraph==0.2.62
langchain==0.3.25
langchain-core==0.3.58

# Vector DB
chromadb==1.1.0
chroma-hnswlib==0.7.5

# PDF parsers
pdfplumber==0.11.7
PyMuPDF==1.24.9

# Retrieval fallback
rank-bm25==0.2.2

# Object storage (MinIO / S3-compatible)
minio==7.2.15
boto3==1.35.36

# MongoDB
pymongo==4.15.0

# Observability
langfuse>=3.0.0

# Images & OCR
pytesseract==0.3.13
Pillow==10.4.0